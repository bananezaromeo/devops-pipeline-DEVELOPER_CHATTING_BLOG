// REGISTERasync function registerUser() {    const username = document.getElementById("username").value;    const email = document.getElementById("email").value;    const password = document.getElementById("password").value;    const res = await request("/signup", "POST", { username, email, password });    document.getElementById("msg").innerText = res.message || "OTP sent!";    if (res) window.location.href = "otp.html";}// VERIFY OTPasync function verifyOtp() {    const email = document.getElementById("email").value;    const otp = document.getElementById("otp").value;    const res = await request("/verify-otp", "POST", { email, otp });    document.getElementById("msg").innerText = res.message || "OTP verified!";    if (res) window.location.href = "login.html";}// LOGINasync function loginUser() {    const email = document.getElementById("email").value;    const password = document.getElementById("password").value;    const res = await request("/signin", "POST", { email, password });    if (res.token) {        localStorage.setItem("token", res.token);        window.location.href = "chat.html";    } else {        document.getElementById("msg").innerText = res.message || "Login failed";    }}